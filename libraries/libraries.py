# from random import shuffle

# cards = ["Jack", "Queen", "King"]
# shuffle(cards)
# for card in cards:
#     print(card)


# from random import randint
# number = randint(1, 10)
# print(number)

## imports everything in the module, not just the random.choice:
# import random
## need to specify function i.e. coin = random.choice(["Heads", "Tails"])

## imports the exact function from the module, not the whole module:
    # from random import choice
    ## now we can just call the function by name
    # coin = choice(["Heads", "Tails"])
    # print(coin)

